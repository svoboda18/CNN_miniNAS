# Auto-generated by CNN_Cls_miniNAS
import torch
import torch.nn as nn

class BestModel(nn.Module):
    def __init__(self, flat_size, num_classes=10):
        super().__init__()
        self.features = nn.Sequential(
            nn.Conv2d(1, 3, kernel_size=2, padding=0, padding_mode='zeros'),
            nn.Dropout(0.3),
            nn.Conv2d(3, 5, kernel_size=3, padding=1, padding_mode='zeros'),
            nn.Conv2d(5, 5, kernel_size=3, padding=1, padding_mode='zeros'),
            nn.Conv2d(5, 3, kernel_size=3, padding=1, padding_mode='zeros'),
        )

        self.classifier = nn.Sequential(
            nn.Flatten(),
            nn.Linear(flat_size, num_classes),
        )

    def forward(self, x):
        x = self.features(x)
        return self.classifier(x)